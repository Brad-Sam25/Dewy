// import * actionType from ''

const authReducer = ( state = {  authData: null }, action) => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}