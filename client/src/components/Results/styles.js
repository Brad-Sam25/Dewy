import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  resultBox: {
    marginBottom: "50px",
  },
  avtr: {
    height: "56px",
    width: "56px",
    backgroundColor: "teal",
  },
  imgSize: {
    objectFit: "contain",
    height: "500px",
    width: "100%"
  }
});
